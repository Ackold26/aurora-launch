# Aurora Launch — frontend .env example. Copy to .env (git-ignored).
#
# Build profile gate (Block 1D B1): production releases set this в CI.
# Local dev: leave unset (defaults к "dev" via build.rs).
# AURORA_BUILD_PROFILE=production

# Tauri devtools enable (only honoured в debug builds)
# TAURI_ENV_DEBUG=1
