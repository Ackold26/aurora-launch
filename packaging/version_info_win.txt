# PyInstaller VSVersionInfo для aurora-sidecar.exe (Windows metadata).
#
# POST_PILOT_BACKLOG M2-PYINSTALLER-5 close (2026-05-10): without version_file
# Windows Properties → Details показывал generic info (no Company / Description /
# Copyright) — cosmetic but breaks pilot trust signals.
#
# Bump filevers + prodvers tuples при каждом version bump в pyproject.toml.
# Single-source TODO Phase B: generate этот файл из pyproject.toml в CI.

VSVersionInfo(
  ffi=FixedFileInfo(
    # Format: (major, minor, patch, build) — 4 ints. Beta releases use
    # build=0; CI release pipeline bumps build for non-tagged dev builds.
    filevers=(0, 1, 0, 0),
    prodvers=(0, 1, 0, 0),
    mask=0x3f,
    flags=0x0,
    # OS=0x40004 = NT, Windows32
    OS=0x40004,
    # fileType=0x1 = VFT_APP (executable application)
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        # Lang code 040904B0 = US English, Unicode codepage
        u'040904B0',
        [
          StringStruct(u'CompanyName', u'Aurora AI'),
          StringStruct(u'FileDescription', u'Aurora Launch — Bayesian forecast sidecar'),
          StringStruct(u'FileVersion', u'0.1.0.0'),
          StringStruct(u'InternalName', u'aurora-sidecar'),
          StringStruct(u'LegalCopyright', u'© 2026 Aurora AI. All rights reserved.'),
          StringStruct(u'OriginalFilename', u'aurora-sidecar.exe'),
          StringStruct(u'ProductName', u'Aurora Launch'),
          StringStruct(u'ProductVersion', u'0.1.0.0'),
          StringStruct(u'Comments', u'IPC sidecar daemon for Aurora Launch desktop app. Communicates via JSON-RPC stdin/stdout.'),
        ]
      )
      ]
    ),
    VarFileInfo([VarStruct(u'Translation', [0x409, 1200])])
  ]
)
