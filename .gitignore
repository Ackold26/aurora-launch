# OS
Thumbs.db
.DS_Store
desktop.ini

# Editor
.idea/
.vscode/
*.swp
*~

# Backup files
*.bak
*.tmp
~$*

# Office locks
.~lock.*

# Future: build artifacts (Phase B+ when code added)
__pycache__/
*.pyc
node_modules/
target/
dist/
build/
.venv/
.coverage
htmlcov/
.pytest_cache/
.hypothesis/

# Tauri build artefacts (CI-produced, never commit locally)
src-tauri/binaries/
src-tauri/gen/
# Иконки (1.8): tracked в repo, регенерируются из мастер-лого Aurora
# через `python tools/generate_icons.py`. Раньше всё подряд игнорилось,
# из-за чего CI собирал app без бренда (placeholder 187 байт).

# Future: secrets / local configs (do not commit)
.env
.env.local
*.key
*.pem
license.json
aurora-secrets.env

# Future: project test data with client info (privacy)
TestData/
fixtures/private/
*.aurora

# Sprint B4: generated report deliverables are build output, never committed
*.pptx
launch_forecast_sample*
